# Python
__pycache__/
*.pyc
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
dist/
build/

# Node
node_modules/
frontend/dist/

# Tauri (added in W7)
src-tauri/target/

# Local-only real-instrument test data (fetched on demand)
tests/data/realdata/

# Per-machine planning docs — NOT tracked (fermi-viewer convention).
# plans/PORT_PLAN.md, plans/PORT_CHECKLIST.md, design/handoff/ (frontend
# spec + prototype HTML) all live here locally; copy between machines
# manually or via OneDrive.
plans/
design/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db
desktop.ini

# TS incremental build state
*.tsbuildinfo
